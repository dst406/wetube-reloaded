extends base.pug

block content 
    form(method="POST")
        input(placeholder="이메일", name="email", type="text", required, value=loggedInUser.name)
        input(placeholder="이름", name="name", type="text", required, value=loggedInUser.email)
        input(placeholder="아이디", name="username", type="text", required, value=loggedInUser.username)
        input(placeholder="지역", name="location", type="text", required, value=loggedInUser.location)
        input(type="submit", value="수정하기")
        if !loggedInUser.socialOnly
            hr
            a(href="change-password") 비밀번호 변경 &rarr;