extends ../base

block content 
    if errorMessage 
        span=errorMessage
    form(method="POST")
        input(placeholder="기존 비밀번호",type="password", name="oldPassword")
        input(placeholder="새 비밀번호",type="password", name="newPassword")
        input(placeholder="비밀번호 확인",type="password", name="passwordConfirm")
        input(value="비밀번호 변경" type="submit")