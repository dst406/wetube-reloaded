extends base.pug
include mixins/video

block content 
    form(method="GET")
        input(placeholder="제목으로 검색하세요", name="keyword", type="text")
        input(type="submit" value="Search now")

    each video in videos 
        +video(video)
